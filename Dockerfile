# 1. استفاده از یک ایمیج رسمی و سبک پایتون
FROM python:3.9-slim

# 2. تنظیم پوشه کاری داخل کانتینر
WORKDIR /app

# 3. کپی کردن فایل نیازمندی‌ها و نصب آن‌ها
# این کار برای استفاده از کش لایه‌های داکر بهینه است
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. کپی کردن بقیه فایل‌های پروژه
COPY . .

# 5. دستوری که باید اجرا شود
# این دستور اسکریپت پایتون شما را اجرا می‌کند
CMD ["python", "monitor.py"]
